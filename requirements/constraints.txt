# Version constraints for pip-installation.
#
# This file doesn't install any packages. It specifies version constraints
# that will be applied if a package is needed.
#
# When pinning something here, please provide an explanation of why.  Ideally,
# link to other information that will help people in the future to remove the
# pin when possible.  Writing an issue against the offending project and
# linking to it here is good.

# Common constraints for edx repos
-c https://raw.githubusercontent.com/edx/edx-lint/master/edx_lint/files/common_constraints.txt


# --- Pinned to match fccn/openedx-platform (nau/teak.master) ---
# These pins mirror the exact versions locked in requirements/edx/base.txt on
# fccn/openedx-platform's nau/teak.master branch, so corporate's CI/build
# resolves the same shared-package versions as the actual deployed platform.
# Update these whenever nau/teak.master's base.txt changes these packages.
Django==4.2.20
# Lower bound: drf-nested-routers==0.94.2 requires djangorestframework>=3.15.0
# Upper bound: djangorestframework==3.18.0 drops Django 4.2 support entirely,
# which is incompatible with the Django==4.2.20 pin above (see nau-technical#1019)
djangorestframework>=3.15.0,<3.18.0
celery==5.5.1
edx-organizations==6.13.0
openedx-events==10.2.0
openedx-filters==2.0.1
edx-opaque-keys==3.0.0

# Fix click version conflict between pip-tools (8.1.8) and edx-lint/quality requirements (8.2.1)
# Using the newer version to ensure compatibility with edx-lint
click==8.2.1

# Fix packaging version conflict between pip-tools (24.2) and other requirements (25.0)
# Using the newer version for consistency across all requirements
packaging==25.0

# Pin cachetools to avoid version conflicts between tox (6.1.0) and our direct dependency (6.2.1)
# Using the newer version for better compatibility and features
cachetools==6.2.1
